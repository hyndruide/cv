import 'package:flutter/material.dart';

class Resume extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Premier test",
          style: TextStyle(color: Colors.white, fontSize: 20.0)),
    );
  }
}
